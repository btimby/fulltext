language: python
python:
  - "2.7"
  - "3.5"
before_install:
  - make sysdeps
  - make pydeps
install:
  - pip install --timeout=60 -q -e .
before_script:
  - make lint
  - python -m fulltext -t check
script:
  - make ci
