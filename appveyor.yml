build: false

environment:
  matrix:
    - PYTHON: "C:\\Python35"
      PYTHON_VERSION: "3.5.x"
      PYTHON_ARCH: "64"

init:
  - "ECHO %PYTHON% %PYTHON_VERSION% %PYTHON_ARCH%"

install:
  - "%WITH_COMPILER% %PYTHON%/python fulltext/data/winmake.py ci"

skip_commits:
  message: skip-ci
